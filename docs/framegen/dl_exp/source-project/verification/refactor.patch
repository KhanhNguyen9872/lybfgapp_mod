warning: in the working copy of 'C:\Users\Admin\Documents\GitHub\lybfgapp_mod\scratch\dl_exp_arm64_reconstruct_research\full_source_generated\rebuild\jni\reconstructed_module.cpp', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'C:\Users\Admin\Documents\GitHub\lybfgapp_mod\docs\framegen\dl_exp\source-project\jni\src\module_runtime.cpp', LF will be replaced by CRLF the next time Git touches it
diff --git "C:\\Users\\Admin\\Documents\\GitHub\\lybfgapp_mod\\scratch\\dl_exp_arm64_reconstruct_research\\full_source_generated\\rebuild\\jni\\reconstructed_module.cpp" "C:\\Users\\Admin\\Documents\\GitHub\\lybfgapp_mod\\docs\\framegen\\dl_exp\\source-project\\jni\\src\\module_runtime.cpp"
index 619a459..99cb75c 100644
--- "C:\\Users\\Admin\\Documents\\GitHub\\lybfgapp_mod\\scratch\\dl_exp_arm64_reconstruct_research\\full_source_generated\\rebuild\\jni\\reconstructed_module.cpp"
+++ "C:\\Users\\Admin\\Documents\\GitHub\\lybfgapp_mod\\docs\\framegen\\dl_exp\\source-project\\jni\\src\\module_runtime.cpp"
@@ -1,194 +1,68 @@
-// Generated-name behavioral reconstruction of the analyzed ARM64 module.
-// The retained entry points and the companion wire shape are derived from the
-// Ghidra feature-path export and the raw ARM64 call map.
+#include "module_runtime.hpp"
 
-#include <android/log.h>
-#include <dlfcn.h>
-#include <errno.h>
-#include <fcntl.h>
-#include <poll.h>
-#include <sys/ioctl.h>
 #include <sys/socket.h>
-#include <sys/types.h>
 #include <unistd.h>
 
-#include <atomic>
-#include <cstdint>
-#include <cstring>
+#include "external_provider.hpp"
+#include "module_state.hpp"
+#include "project_config.hpp"
+#include "wire_protocol.hpp"
 
-#include "zygisk.hpp"
+namespace reconstructed {
 
-namespace {
-
-constexpr char kLogTag[] = "lybfghook-reconstructed";
-constexpr char kExternalHookLibrary[] = "liblybfghook.so";
-constexpr char kHookSymbolA[] = "FunctionInlineHookRouting";
-constexpr char kHookSymbolB[] = "FunctionInlineHook";
-constexpr char kControlDevice[] = "/dev/lybfghook";
-constexpr uint32_t kWireMagic = 0x4b484746;  // generated protocol marker
-constexpr uint32_t kWireVersion = 1;
-constexpr unsigned kReadTimeoutMs = 250;
-
-struct WireStatus {
-    uint32_t magic;
-    uint32_t version;
-    uint32_t state;
-    uint32_t provider_loaded;
-    uint32_t control_ioctl;
-};
-
-struct FeatureState {
-    zygisk::Api *api = nullptr;
-    JNIEnv *env = nullptr;
-    void *hook_library = nullptr;
-    void *hook_symbol = nullptr;
-    uint32_t control_ioctl = 0;
-    std::atomic<uint32_t> initialized{0};
-    std::atomic<uint32_t> companion_requests{0};
-};
-
-FeatureState &state() {
-    static FeatureState instance;
-    return instance;
+void Module::onLoad(zygisk::Api *api, JNIEnv *env) {
+    RuntimeState &state = runtime_state();
+    state.api = api;
+    state.env = env;
+    provider::ensure_loaded();
+    log_debug("onLoad");
 }
 
-void log_message(const char *message) {
-    __android_log_write(ANDROID_LOG_DEBUG, kLogTag, message);
-}
-
-void resolve_external_provider() {
-    FeatureState &current = state();
-    if (current.initialized.exchange(1, std::memory_order_acq_rel) != 0) {
-        return;
-    }
-
-    // The analyzed companion path resolves this library dynamically rather
-    // than listing it in DT_NEEDED. Keep that load boundary intact.
-    current.hook_library = dlopen(kExternalHookLibrary, RTLD_NOW | RTLD_LOCAL);
-    if (current.hook_library != nullptr) {
-        current.hook_symbol = dlsym(current.hook_library, kHookSymbolA);
-        if (current.hook_symbol == nullptr) {
-            current.hook_symbol = dlsym(current.hook_library, kHookSymbolB);
-        }
-    }
+void Module::preAppSpecialize(zygisk::AppSpecializeArgs *args) {
+    record_process(args == nullptr ? nullptr : args->nice_name);
+    request_companion();
 }
 
-void probe_control_device() {
-    FeatureState &current = state();
-    int fd = open(kControlDevice, O_RDWR | O_CLOEXEC);
-    if (fd < 0) {
-        return;
-    }
-
-    // The constant and the 0x100-byte request area are visible in the
-    // recovered companion function. The device path remains generated because
-    // the original stores it in an obfuscated data object.
-    alignas(8) unsigned char request[0x100] = {};
-    if (ioctl(fd, 0xc0180902, request) == 0) {
-        current.control_ioctl = 1;
-    }
-    close(fd);
+void Module::postAppSpecialize(const zygisk::AppSpecializeArgs *) {
+    log_debug("postAppSpecialize");
 }
 
-bool write_all(int fd, const void *data, size_t size) {
-    const unsigned char *cursor = static_cast<const unsigned char *>(data);
-    while (size != 0) {
-        ssize_t written = write(fd, cursor, size);
-        if (written <= 0) {
-            if (errno == EINTR) {
-                continue;
-            }
-            return false;
-        }
-        cursor += written;
-        size -= static_cast<size_t>(written);
-    }
-    return true;
+void Module::preServerSpecialize(zygisk::ServerSpecializeArgs *) {
+    record_process(nullptr);
+    request_companion();
 }
 
-void drain_request(int fd) {
-    struct pollfd descriptor = {fd, POLLIN, 0};
-    if (poll(&descriptor, 1, static_cast<int>(kReadTimeoutMs)) <= 0) {
-        return;
-    }
-    unsigned char request[20] = {};
-    (void)recvfrom(fd, request, sizeof(request), MSG_DONTWAIT, nullptr, nullptr);
+void Module::postServerSpecialize(const zygisk::ServerSpecializeArgs *) {
+    log_debug("postServerSpecialize");
 }
 
-void reconstructed_companion(int client) {
-    FeatureState &current = state();
-    current.companion_requests.fetch_add(1, std::memory_order_relaxed);
-    resolve_external_provider();
-    probe_control_device();
-
-    WireStatus status = {
-        kWireMagic,
-        kWireVersion,
-        current.initialized.load(std::memory_order_acquire),
-        current.hook_symbol == nullptr ? 0u : 1u,
-        current.control_ioctl,
-    };
-    (void)write_all(client, &status, sizeof(status));
-    drain_request(client);
-    close(client);
+void Module::record_process(jstring process_name) {
+    log_process_name(runtime_state().env, process_name);
 }
 
-class ReconstructedModule final : public zygisk::ModuleBase {
-public:
-    void onLoad(zygisk::Api *api, JNIEnv *env) override {
-        state().api = api;
-        state().env = env;
-        resolve_external_provider();
-        log_message("onLoad");
-    }
-
-    void preAppSpecialize(zygisk::AppSpecializeArgs *args) override {
-        record_process(args == nullptr ? nullptr : args->nice_name);
-        request_companion();
-    }
-
-    void postAppSpecialize(const zygisk::AppSpecializeArgs *) override {
-        log_message("postAppSpecialize");
-    }
-
-    void preServerSpecialize(zygisk::ServerSpecializeArgs *) override {
-        record_process(nullptr);
-        request_companion();
-    }
-
-    void postServerSpecialize(const zygisk::ServerSpecializeArgs *) override {
-        log_message("postServerSpecialize");
+void Module::request_companion() {
+    RuntimeState &state = runtime_state();
+    if (state.api == nullptr) {
+        return;
     }
 
-private:
-    void record_process(jstring process_name) {
-        if (process_name == nullptr || state().env == nullptr) {
-            return;
-        }
-        const char *name = state().env->GetStringUTFChars(process_name, nullptr);
-        if (name != nullptr) {
-            __android_log_print(ANDROID_LOG_DEBUG, kLogTag, "process=%s", name);
-            state().env->ReleaseStringUTFChars(process_name, name);
-        }
+    const int client_socket = state.api->connectCompanion();
+    if (client_socket < 0) {
+        return;
     }
 
-    void request_companion() {
-        if (state().api == nullptr) {
-            return;
-        }
-        int client = state().api->connectCompanion();
-        if (client < 0) {
-            return;
-        }
-        WireStatus request = {kWireMagic, kWireVersion, 1, 0, 0};
-        (void)write_all(client, &request, sizeof(request));
-        WireStatus response = {};
-        (void)recv(client, &response, sizeof(response), MSG_WAITALL);
-        close(client);
-    }
-};
+    const protocol::StatusPacket request = {
+        config::kWireMagic,
+        config::kWireVersion,
+        1,
+        0,
+        0,
+    };
+    (void)protocol::write_all(client_socket, &request, sizeof(request));
 
-}  // namespace
+    protocol::StatusPacket response = {};
+    (void)protocol::read_packet(client_socket, &response);
+    close(client_socket);
+}
 
-REGISTER_ZYGISK_MODULE(ReconstructedModule)
-REGISTER_ZYGISK_COMPANION(reconstructed_companion)
+}  // namespace reconstructed
